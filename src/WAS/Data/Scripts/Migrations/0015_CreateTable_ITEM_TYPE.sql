-- 0015: ITEM_TYPE 테이블 생성 (품목 유형 마스터)

CREATE TABLE ITEM_TYPE (
    ITEM_TYPE_ID  NUMBER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    TYPE_NAME     VARCHAR2(100) NOT NULL,
    CREATED_AT    TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
/
