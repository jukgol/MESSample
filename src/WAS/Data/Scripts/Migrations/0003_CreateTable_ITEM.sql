-- 0003: ITEM 테이블 생성 (자재 마스터)
CREATE TABLE ITEM (
    item_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    item_name VARCHAR2(100) NOT NULL,
    category VARCHAR2(50),
    unit VARCHAR2(20),
    description CLOB,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
