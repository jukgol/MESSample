-- 0031: Allow logical deletion for process step executions
ALTER TABLE PROCESS_STEP_EXECUTION DROP CONSTRAINT CHK_PSE_STATUS;
/
ALTER TABLE PROCESS_STEP_EXECUTION ADD CONSTRAINT CHK_PSE_STATUS
CHECK (status IN ('WAITING', 'RUNNING', 'PAUSED', 'DONE', 'FAILED', 'DELETED'));
/
