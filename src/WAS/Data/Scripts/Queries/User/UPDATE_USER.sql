UPDATE USER_INFO 
SET LOGIN_ID = :LOGIN_ID,
    PASSWORD = :PASSWORD,
    USER_NAME = :USER_NAME,
    ROLE_CODE = :ROLE_CODE,
    IS_ACTIVE = :IS_ACTIVE,
    UPDATED_AT = CURRENT_TIMESTAMP 
WHERE USER_ID = :UserId