[tool.poetry]
name = "mes-test-tool"
version = "0.1.0"
description = "MES monitoring test tool UI"
authors = ["MES Sample"]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.11,<4.0"
streamlit = ">=1.36.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
